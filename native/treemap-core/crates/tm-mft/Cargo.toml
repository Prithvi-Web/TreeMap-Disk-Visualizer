[package]
name = "tm-mft"
description = "The NTFS master file table, read as records: the FILE-record parser, the run-list decoder and the tree builder, portable and tested on synthetic records."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
tm-walk = { path = "../tm-walk" }
