# Risks (Phase 0)

What could break, what is hard to test, what is platform-specific, and what
could cost a user data, disk space or bandwidth. Each risk names the phase
that retires it and how. Severity is the cost to a user if it lands, not the
likelihood. Companion to [`CURRENT-STATE.md`](CURRENT-STATE.md) and
[`DESIGN.md`](DESIGN.md).

## A. Data, disk and bandwidth (the ones that matter most)

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R1 | **The legacy duplicate finder hashed cloud placeholders.** `duplicateFinder.ts` filtered nothing by flag; an evicted iCloud Drive or OneDrive file of a bucketed size was opened and read, which makes the OS download it — and on Windows a cloud file is a reparse point both engines record as a link, which a read follows | CRITICAL — bandwidth, a metered connection, and the disk the user is trying to empty | **Retired 23 Sep 2026**, pulled forward from Phase 5 with the owner's approval: the exact finder never opens a placeholder or a link (a test watches every open), counts and names the placeholders it left in `notHashed` (the page, the API and the MCP tool say so), and the native engine marks every entry its walk flags dataless as a placeholder (DESIGN §16 item 10). A file evicted after the scan is R71 | — |
| R2 | **The walker still guesses which files are placeholders.** It cannot read the dataless flag, so it marks a file with a size, nothing allocated and a path under a known cloud folder; a dataless file elsewhere (another sync client's folder) is not marked in a walker scan (an incremental rescan is one) | HIGH | The native engine, the default where it loads, uses the flag itself (23 Sep 2026); FUSE mounts are not yet treated as read-hostile | The check at read time (R71, done 23 Sep 2026) closes it for the duplicate pass whatever engine scanned; FUSE with the Linux mount probe |
| R3 | **Spill files fill the disk the user is cleaning.** A 100M-entry index is ~3.75 GB on disk | HIGH | Design: 3× free-space check before spilling, automatic switch to aggregate mode with a notice, app-data volume excluded from its own totals | Phase 4, with a test that fakes a nearly full volume |
| R4 | **Spill files outlive the scan** after a crash | MEDIUM | Design: startup sweep by age, removal on expiry and quit | Phase 4; a test plants an orphan and starts the app |
| R5 | **A cache causes a delete.** The digest cache or the arena index could feed a stale path to the cart | CRITICAL | Rule: caches accelerate; every destructive endpoint re-validates against the live filesystem immediately before acting, as `requireInsideScanRoot` already does for roots | Phase 5; the last-copy rule also becomes server-side with a test |
| R6 | **Deleting one hard link or clone reported as reclaimable** | HIGH | Hard links: `(dev, ino)` side table, family reported as sharing storage. Clones: no public API reports clone identity on APFS (`docs/PLATFORM_NOTES.md:80-88`); the response says the figure is an upper bound rather than pretending | Phase 5; a test plants links and clones |
| R7 | **Offload verification drift.** Two digests in two places (`copyWithHash`, the catalogs) with no algorithm field on disk | LOW (nothing changes) | Design D5: offload and Time Capsule keep SHA-256; if that ever changes, the catalog gets an `algo` field first | — |

## B. Correctness of accounting

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R8 | **The native tree silently differs from the legacy tree** (a size off by a block, a missed entry, a firmlink descended twice) | CRITICAL — the app's whole reputation | The prompt's absolute gate | Phase 3's equivalence digest on every corpus and every edge-case fixture; any difference is a bug until listed in `DESIGN.md` §16 |
| R9 | **`ATTR_CMN_RETURNED_ATTRS` ignored** → garbage sizes on a filesystem that returns fewer attributes | HIGH | The vendored `bulk.rs` honours it and falls back to `lstat` for an entry the kernel would not describe; the mobile review found one path where a malformed size became a measured zero | Phase 3 re-fixes it here with a test that feeds a truncated attribute buffer |
| R10 | **NFC/NFD twins on macOS double-count.** Today nothing normalises; HFS+ normalised names on disk and APFS does not | MEDIUM | Both engines store names as returned, so the equivalence test agrees on the wrong answer | Phase 3 fixture generator creates both spellings; the decision (count once, report the pair) is recorded in `DESIGN.md` §16 when taken |
| R11 | **Windows has no allocation accounting today** (`blocksAreMeaningful` false) and the native path will report `AllocationSize` for the first time | MEDIUM — a number that used to be absent appears | Additive: fields that were 0 become measured; the missing-gigabytes statement already treats Windows separately | Phase 3 Windows; CI's Windows leg |
| R12 | **Vanished-mid-walk and permission-denied handling regresses** (a refused folder rendered as empty) | HIGH | The mobile walker was built around the opposite rule; the desktop counts them | Phase 3 tests plant a mode-000 directory and remove one mid-walk |
| R13 | **A file that grows while being hashed** produces a wrong digest | LOW | Sample digest includes the size; full digest re-checks size after reading | Phase 5 test |
| R14 | **Sub-second mtimes lost** in a `u32` seconds column break the incremental rule | MEDIUM | Side table for exact nanoseconds; the reuse key uses the exact value | Phase 4 invalidation suite |
| R62 | **The NTFS turbo mode's cross-check counts an entry it cannot find as a skip, not a divergence.** An entry the table lists that is gone when the app opens it may have been deleted since the read — or may never have existed, which a table the app should refuse would show; the two are not told apart | MEDIUM | The gate still needs half its eligible entries (at most 1,000) opened and matching (`requiredMatches`), so a table made mostly of phantoms is refused; a few among real entries pass | An entry not found whose parent folder was last written before the read, so that nothing could have deleted it since, counts as a divergence (the pre-landing review of 23 Sep 2026) |

## C. Platform and environment

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R15 | **macOS warm-cache targets above ~250k entries cannot be met on a default install** (`kern.maxvnodes = 251,127` here). The prompt's headline 1M/s is a warm-cache figure | HIGH — a target that would be reported as missed, or worse, faked | Stated in `DESIGN.md` §1; the harness labels cache state per run and reports the measured number | Phase 1 measures; README says cold and large scans are hardware-bound |
| R16 | **A cold-cache run cannot be labelled cold without root.** `purge` needs `sudo` on this macOS; the harness may not have it | MEDIUM | The runner refuses to label a run "cold" unless the purge procedure ran and exited 0; otherwise the run is "unknown cache state" and excluded from cold targets | Phase 1 |
| R17 | **`getattrlistbulk` returns `ENOTSUP`** on SMB, some FUSE and some third-party volumes | MEDIUM | Capability probe + per-directory fallback to `fdopendir`/`fstatat` | Phase 3, tested against a mounted disk image and a synthetic `ENOTSUP` |
| R18 | **TCC: a folder macOS refuses to the app.** An `lstat` passes, `open` fails; the legacy preflight opens the directory to be honest | HIGH | The native walker keeps the "a refused root is a failed scan, never an empty one" rule | Phase 3 |
| R19 | **Windows long paths, junctions, reparse points, `MAX_PATH`** | MEDIUM | `\\?\` prefix internally, `FILE_FLAG_OPEN_REPARSE_POINT`, reparse points as leaves | Phase 3 Windows; the edge-case generator; CI's Windows leg is the only place it runs for real |
| R20 | **Linux `io_uring` blocked by seccomp or absent in containers**; cgroup limits ignored | MEDIUM | Runtime detection with synchronous `statx` fallback; `cpu.max` read for the concurrency ceiling | Phase 3 Linux |
| R21 | **Firmlinks and bind mounts.** `/System/Volumes/Data` is the same tree as `/`; a device-id rule alone is wrong on macOS | HIGH | Keep the never-descend list *and* the device check, and visited `(dev, ino)` for directories | Phase 3; the firmlink fixture already exists in `pathSanitizer` tests |
| R22 | **The developer machine's C toolchain**: `cc` alone cannot link (command-line-tools SDK newer than Xcode 26.6's linker). `cargo` links fine | LOW (developer only) | The harness compiles its one C probe with `xcrun --sdk macosx clang -isysroot …` | Phase 1 |
| R23 | **macOS 27 resets loopback connections beyond the listen backlog**, which is why one existing test is red locally | LOW (test only) | Recorded in `CURRENT-STATE.md` §13 | Phase 1's test fix (≤ 64 sockets, same 200 requests) |
| R53 | **A volume mount point is walked when it is the scan root, or when a directory is swapped for one after its parent was listed.** `open_dir` in `windows.rs` reopens through a `MOUNT_POINT` reparse point whose substitute name is not `\??\X:`; a child mount point is a size-0 symlink-kind leaf that is never entered | LOW | Mirrors the legacy walker exactly: `fs.lstat` on such a junction reports a directory (libuv treats only `\??\X:` junctions as links) and libuv's `scandir` types every reparse child as a link, so both engines walk a root mount point and neither enters a child one. The 22 Sep 2026 security review rated this CRITICAL on the assumption that children were followed — they are not; the exposure is metadata of a volume the same user can already read, and every destructive action re-validates the live path | The Windows CI leg's first live run (question (b) of the eight); a root-at-mount-point case needs a second volume the runner may not have |
| R54 | **A lone UTF-16 surrogate in a Windows name is U+FFFD or WTF-8 depending on the libuv inside the Node that runs the legacy walker.** libuv before 1.47 replaces it (as `windows.rs` does); from 1.47 it keeps the surrogate as WTF-8 bytes. CI's Node 20 and the desktop app's Electron may not agree with each other | LOW | Unprovable here, and the edge fixture cannot plant such a name from Node (the JS string is converted on its way in). The rule and its reason stand at the top of `Record::name_string` | A live Windows comparison; if a leg shows a digest difference on such a name, the decision is per Node version |
| R55 | **A volume without file ids (the `FindFirstFileExW` fallback: FAT32, exFAT, some shares) reports every leaf as `withheld`, so `unreadableEntries = fileCount`** — true to the crate's contract, false to the user, whose files were all read | MEDIUM | The owner's decision, asked 22 Sep 2026. The recommendation: a missing file id (which only feeds the hard-link rule, and such volumes have no hard links) and a missing allocation (never meaningful on Windows, R-legacy: libuv leaves `blocks` at 0) do not make an entry unreadable; only a missing size or mtime does | **Retired 23 Sep 2026** by the owner’s word: `stage_record` no longer withholds for a missing allocation or file id, and the walk keys no file id of 0 as a hard link (`files_without_a_file_id_are_never_one_hard_link_family` — unwithheld, the whole volume would have been one family); tm-mft withholds a record without `$DATA` or `$STANDARD_INFORMATION` explicitly, as before |
| R63 | **A `subst` drive, or one mapped only in this session, passes the drive check and fails in the helper** | LOW | The elevated helper runs in the administrator's session, which has its own drive letters, so it refuses the letter after the yes; that failure switches the mode off for the drive until TreeMap restarts, so it costs one prompt | `QueryDosDeviceW` in `mftPrecheck`, to name a substituted or session-mapped letter before anyone is asked |
| R64 | **Cancelling a scan does not close its elevation question.** The app's question (and, after a yes, Windows' prompt) stays on screen until answered and holds the one prompt slot, so another scan meanwhile scans without the mode | LOW | The cancelled scan never uses the answer (`runMftWalk` checks `scan.cancelled` after the prompt); the slot is a rule, not a failure, and the other scan says why | Close the app's own question on cancel (`dialog.showMessageBox`'s `signal`); Windows' prompt cannot be withdrawn |

## D. Resource governor

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R24 | **The budget is a thread count, not a ceiling.** Without QoS, I/O policy and duty cycling, "Eco" is a label | HIGH — the user's primary ask | **Retired 18 Sep 2026**: QoS per worker thread, a sleep ledger per thread, worker shedding, and the loop held 22.6 / 47.0 / 89.2 % against 25 / 50 / 90 for 60 s each (DESIGN §8.1). Open remainder: Eco and Balanced have no headroom on 8 cores (R52a) | Phase 2 (done) |
| R25 | **Only Tier B exists here.** Tier A and Tier C bands cannot be verified on this Mac | MEDIUM | Reported as "not available on this machine", never as passed; CI runners (2–4 vCPU) stand in for Tier C with their noise stated | Phase 2 |
| R26 | **The legacy engines cannot hold Eco exactly** (Node cannot set QoS or I/O policy; gdu is a child process) | MEDIUM | Built as the Node shim: a per-worker duty clock, a worker cap, `nice` on gdu shards, and every answer says `source: 'node-shim'`; a governor that loaded but is not in force is reported as the shim too, never as a measurement | Phase 2 (done); the native walker's own workers are governed directly in Phase 3 |
| R27 | **Thermal and battery signals need Objective-C/IOKit calls from Rust** | LOW | `objc2-foundation` and IOKit through the `core-foundation` crates; a synthetic thermal state drives the tests | Phase 2 |
| R28 | **Sleep/wake**: a scan that keeps a drive awake, or resumes into a vanished mount | MEDIUM | Pause on will-sleep, resume from checkpoint on wake, re-probe the root | Phase 2/3 |
| R52a | **Eco and Balanced sit at duty 1.0 on 8 cores** (`max_workers × 1.0 / cores` equals the ceiling), so a competing process pulls the share below target with nothing the loop can add; measured as an 8 s Eco dip to 0.16–0.19 while `syspolicyd` ran, inside the band by 2.0–2.6 points | LOW | Recorded with the measurement in DESIGN §8.1; a third Eco worker on ≥ 8 cores is the candidate fix, to be measured, not assumed | Phase 8 tuning |

## E. Memory and the 100M path

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R29 | **RSS counts mapped file pages.** A column written through `mmap` stays resident on a machine with free RAM and fails the gate while harmless | MEDIUM | `write()` during the walk, read-only `mmap` afterwards, page-touch budget for queries (`DESIGN.md` §6.2) | Phase 4 measurement |
| R30 | **The aggregate-mode budget is 4 MB under its 400 MB ceiling on paper** | MEDIUM | Two knobs (interner cap, transport prune) reserved; defaults chosen after measurement | Phase 4 |
| R31 | **Name interning at 100M entries** needs a hash set that itself grows | LOW | Capped; beyond the cap names are stored raw | Phase 4 |
| R32 | **The Node side still materialises object trees** (`scan.root` getter; the 300k-node mtime cache did until 23 Sep 2026) | LOW (bounded) | The mtime cache is streamed from the store (`streamTreeJson`: no object tree, no whole string; measured 69 + 80 ms of event-loop freeze removed at a 200k scan's completion); the getter stays behind the 250k prune | Phase 4 |
| R65 | **The NTFS turbo mode reads and decodes the helper's whole output on Node's main thread** (`mftTake` is synchronous), so the app does not answer while it runs; how long grows with the entry count and is not measured | LOW | One call per turbo scan, after the helper has finished; the cross-check that follows yields between batches | `mftTake` as a napi `AsyncTask` on the libuv pool |
| R66 | **After the walk, each hard-link family's file is read again on one thread** (`refresh_families`), so a tree with many families (`C:\Windows\WinSxS`) adds those reads to the scan's wall time one after another; not measured | LOW | One metadata read per family, cancellable between reads and counted by the heartbeat (the pre-landing review of 23 Sep 2026) | Spread the families over the walk's idle workers |
| R67 | **tm-mft's `RecordTable` holds every in-use record of the volume in the elevated helper's memory** — about 480 bytes a record by the reviewer's estimate from the structure layout, not measured — so a volume of tens of millions of records needs gigabytes | MEDIUM | The helper is a process of its own: running out of memory ends it, not TreeMap, and the scan falls back to the listing walk with the reason | Measure it on a large volume; then keep only the records under the scan root, or pack names into one arena |

## F. Build, distribution and maintenance

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R33 | **A missing or wrong-arch prebuild ships.** Five targets, two of which (macOS x64, Linux arm64) have no CI leg today | HIGH | Each release leg builds its own; new legs added; a target that does not build fails the release, never silently falls back in the installer | Phase 3 per platform, Phase 8 CI |
| R34 | **An end user is asked for a Rust toolchain.** | HIGH | The crate is in no install script; `npm ci` never compiles it; web mode falls back to the legacy chain with a stated reason | Phase 3; a test greps `package.json` scripts for the invariant |
| R35 | **Electron's packaging rebuild trap** (`npm rebuild better-sqlite3` after `electron-builder`) now has a second native module | LOW | N-API modules need no rebuild; the loader's version handshake refuses a mismatched binary with a reason | Phase 3 |
| R36 | **Contributor friction**: Rust in a TypeScript repo | MEDIUM | The native path is optional for development (legacy chain runs without it); `native/README.md` documents the one command; CI builds everything | Phase 3 |
| R37 | **Vendored code drifts from its source** or inherits its open defects | MEDIUM | `VENDORED.md` records the source commit and every change; the review's confirmed findings are re-fixed here with tests before wiring | Phase 3 |
| R38 | **Crate downloads.** The first build fetches `napi`, `napi-derive`, `napi-build`, `crossbeam-deque`, `objc2-foundation`, `windows-sys`, `rustix` and their trees from crates.io; the lockfile pins them | LOW | Recorded in `DESIGN.md` §18 for the owner; `Cargo.lock` is committed; `cargo audit`-style review of the tree before the first commit | Phase 2 |

## G. Measurement honesty

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R39 | **A number printed that no run produced** | CRITICAL to the product's identity | Every UI or README figure must cite a `bench/` run on a named machine under a named cache state; the CI perf gate reads the same files | Phase 8 test that greps README figures against `bench/` output |
| R40 | **Noise read as a result.** The desktop bench today swung +22.5% against its August baseline under load 3.3 | HIGH | The runner prints load average and a resolution band, refuses to call a difference inside the band a change (the existing `bench-v4` rule), takes the median of ≥ 5, and CI uses a fixed small corpus with a 10% threshold | Phase 1 |
| R41 | **The 500 GB duplicate corpus and the 200k-image corpus cannot be built here** (249 GB free; image generation time) | MEDIUM | Scaled corpora with the same distributions, labelled with their scale; ratios (4×, 10× less I/O) are measured, absolute wall-clock targets are reported as "at 1/10 scale" | Phase 1 |
| R42 | **Bytes read is hard to measure on macOS without root.** `fs_usage` needs root | LOW | `proc_pid_rusage(RUSAGE_INFO_V4).ri_diskio_bytesread` works for the same user (verified today) but counts physical reads of the measuring process only, so gdu's reads are `n/a`; Linux `/proc/self/io`; Windows is `null` with the reason | Phase 1 |
| R43 | **Equivalent mutants and tautological tests** — the house's recurring trap (`verification-tooling-traps`) | HIGH | Every new assertion is reddened by a recorded mutant whose anchor count is asserted inside the mutation script | every phase |
| R43a | **A number that includes what it did not mean to.** The first harness measured each pass in one long-lived process: the app's fire-and-forget cache and snapshot writes after a scan landed in the next pass's CPU and bytes, cancelled scans stayed retained for 30 minutes and inflated the lifetime peak RSS, and a requested gdu run could fall back to the walker and be recorded as gdu | HIGH | Retired in Phase 1's review round: every measured pass is a fresh child process, persistence is waited for and reported on its own, the requested engine is asserted on every run, a cold series is `unknown` the moment one purge fails, and `compare` refuses results that differ in corpus, engine, tier, platform or cache state or that failed correctness | Phase 1 |
| R43b | **Corpora on a tmpfs `/tmp`** (some Linux distributions): 11 GB of RAM, and `drop_caches` cannot evict them, so a "cold" label would be false | MEDIUM | `bench/README.md` states the size and the rule; a tmpfs check before labelling a run cold is Phase 8's Linux leg work | Phase 8 |
| R43c | **Windows allocates a `ftruncate`d file in full** (no `FSCTL_SET_SPARSE` from Node), so a sparse corpus would cost its logical size there | MEDIUM | The presets plant no sparse files on Windows (`sparseRate` 0 there, recorded in every manifest's parameters) | Phase 1 |

## H. Security surface of native code

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R44 | **Native code opens files.** A path that escapes the scan root, or a symlink swapped in mid-walk, opened by Rust | CRITICAL | `O_NOFOLLOW` + `O_DIRECTORY` descriptors, `openat` relative to the parent descriptor, never a full-path `open` from a stale listing (the mobile review's CRITICAL, re-fixed here); the core never writes to any volume except app-data; the same `pathGuard` rules apply before a path reaches napi | Phase 3 with a swap-in test |
| R45 | **Unsafe Rust.** `getattrlistbulk` buffer parsing is inherently `unsafe` | HIGH | The workspace lint set (`unsafe_op_in_unsafe_fn = deny`, no `unwrap`/`panic`/`indexing_slicing`), fuzz-style tests over truncated buffers, `panic = unwind` with `catch_unwind` at the napi boundary so a Rust panic becomes a stated engine failure and the legacy engine takes over | Phase 3 |
| R46 | **A panic or crash in the core takes the app down** | HIGH | Every napi entry is wrapped; a crash in the native engine marks it unavailable for the session with the reason and re-runs the scan on the legacy chain | Phase 3 |
| R52 | **A body-less `POST` is a CORS-simple request.** `POST /api/scan/:id/pause` and `/resume` (Phase 2) can be fired by any page a browser on the same machine renders, without a preflight, exactly like the existing `POST /api/compression/:jobId/cancel` and `/autopilot/policies/:id/approve`. The response is opaque and the scan id is a random UUID the page cannot read, so the request needs an id leaked by other means; CORS and the API token stay opt-in as before | LOW | Recorded here as the app's existing posture (loopback listener, unguessable ids, no persisted state touched by these two routes) rather than a one-off guard on two routes; a same-origin check for every state-changing route is a Phase 8 candidate that must land on all of them at once | Phase 8 (decision) |
| R56 | **A cancelled walk whose worker is wedged inside a kernel call cannot be joined**, and until 22 Sep 2026 `scanTake` joined it on Node's main thread — one dead SMB mount would have frozen the whole app | HIGH | After `scanCancel` the app polls up to `NATIVE_CANCEL_DEADLINE_MS` (5 s) for the walk to end, then abandons the handle: the thread and its slot stay in the module for the life of the process, freed only if the call ever returns. A walk that answers no listing batch for `NATIVE_STALL_MS` (30 s, the legacy walker's own per-directory bound) is cancelled the same way and the scan falls back to the legacy engines with that reason in `fallbackReason`. `scanTake` refuses a walk that is still running instead of blocking | An interruptible listing (`pthread_kill` / `CancelSynchronousIo` per platform) — deferred until a measured need |
| R57 | **The prebuilt module's only integrity check is its version string.** A same-architecture `treemap_core.node` tampered with in place, keeping `version()`, loads at full process privilege | LOW | The precondition is write access to the app's own resources, or control of `TREEMAP_NATIVE_MODULE` at launch — the same access that replaces the app itself; a wrong architecture or version fails closed to the legacy engines | A SHA-256 pinned in `package.json` and checked by the loader — a Phase 8 candidate, cheap |
| R58 | **The NTFS turbo mode starts one program as administrator on the person's say-so** | MEDIUM | Only `tm-mft-helper.exe`, one scan at a time, after the app's own question and Windows' prompt; nothing a program running as the user could change is started: PowerShell by its full path under the folder `GetSystemDirectoryW` reports, `elevationRefusal` before anyone is asked, and the helper's and its folder's owners (Administrators, SYSTEM or TrustedInstaller, by security identifier) checked just before `Start-Process` — so the mode works only in an install for anyone who uses the computer. Residual: the prompt itself cannot be proven on CI (W6-9's `not verified on this build` stays), and a program already running as an administrator's own account has simpler routes to elevation than this one — Windows does not treat UAC as a security boundary | W6 (`8bbd8c8`, `b4dc6dc`, three security reviews); a live proof of the prompt |
| R59 | **On Windows, a scan that falls back to gdu counts every hard link's bytes** | MEDIUM | gdu v5.36.1 keys no hard link on Windows (`pkg/analyze/dir_other.go` returns an inode of 0) and dates files from their directory entries, so a tree with hard links — `C:\Windows`, where `WinSxS` and `System32` share thousands of files — is over-counted, possibly by many gigabytes. The native engine (the default on Windows where eligible) and the built-in walker both key hard links by file id; gdu runs on Windows only when the native engine is forced off, missing or fails part-way on a full walk. **Owner's decision:** on Windows, fall back to the walker (correct, slower) instead of gdu | **Retired 23 Sep 2026** by the owner's decision: on Windows a scan the native engine cannot take goes to the walker (`gduRuleFor`), and the scan says why. The first Windows CI run also found gdu refusing every Windows run that passed `-x` (it cannot list mount points there); `gduArgs` passes it only elsewhere |
| R60 | **The persistent index keys hard links by a file id held as a double.** `indexEngine.ts` de-duplicates on `seenInodes.has(entry.ino)` and stores `ino` (a `Stats` double) in SQLite, and the allocation accountant groups families by it. On Windows a reused NTFS record's id passes 2^53 (its sequence number sits in bits 48..64), where two different multi-linked files round to one id: one is counted as the other's duplicate and their families merge | MEDIUM | The scan engines no longer do this (the pre-landing review of 23 Sep 2026: the native engine and the NTFS turbo mode key by the whole id and pass a family number; the walker reads an id past 2^53 again as a bigint). Only files whose link count exceeds one are keyed, so the index's exposure is confined to real hard links (WinSxS, pnpm stores) | Phase 4, with the storage rewrite: the same bigint key for ids past 2^53, stored as an exact integer (better-sqlite3 `safeIntegers`) |
| R61 | **On Windows, an action on a name that ends in a dot or a space can reach a different file.** NTFS keeps such names (made from Linux, or by a program using `\\?\` paths). Node's own file calls reach them exactly, since Node prefixes `\\?\`; but the Recycle Bin call (`cleaner.ts`: `Microsoft.VisualBasic.FileIO.FileSystem.DeleteFile`) normalizes the path the Win32 way and trims the dot or space, so trashing `a.` would recycle `a` if one sits beside it — after `lstat` has checked the right file | MEDIUM | Recoverable (the Recycle Bin), inside the scanned folder, and only for names Windows' own tools cannot make. Names that are not one name at all — `a\b`, `a:b`, empty — are counted unreadable by the listing since 23 Sep 2026 (`stage_record`), so no action is aimed through them; a trailing dot or space is one name to NTFS and is listed | **Retired 23 Sep 2026** (the commit after `506a9a6`): `trashRefusal` refuses, before the Recycle Bin call, a Windows path any part of which ends in a dot or a space, and the page shows why. Open and reveal are not destructive and are left to Windows |
| R72 | **Phase 4 assumed JavaScript could view the Rust store's memory: Electron forbids it.** The desktop app runs in Electron 31, whose V8 memory cage refuses an ArrayBuffer over memory V8 does not own; napi-rs copies instead, so the zero-copy hand-over (DESIGN §4.2, Phase 4 S2) is one copy per column in the desktop app, and a memory-mapped spill (S3) cannot be exposed as typed arrays there at all | MEDIUM | Web mode (plain Node) keeps zero copy; the copy is sequential per column, so the transient peak is the store plus its largest column (to be measured). Measured 23 September 2026 with a throwaway napi probe calling `napi_create_external_arraybuffer`: Node 24.16 answers `napi_ok` (0); the installed TreeMap app's Electron 31.7.7, run as Node (`ELECTRON_RUN_AS_NODE=1`; its RunAsNode fuse is on), answers `napi_no_external_buffers_allowed` (22). The same day, handing the walk's 9.7 MB of columns for enum200k to plain Node grew `process.memoryUsage().arrayBuffers` by 0 MB against a 50 MB control that it counted — zero copy, measured — while in Electron that counter saw neither the hand-over nor the control (Electron allocates ArrayBuffers outside Node's allocator), so Electron's copy must be measured another way (RSS, time). Its cost, measured the same day on enum1m (1,000,002 entries, 48.9 MB of columns, two runs each): `scanTake` took 0.1 ms in Node and 6.3–7.2 ms in Electron (the copy, about 7 GB/s), and the process's peak RSS (`resourceUsage().maxRSS`, the OS counter) did not rise in either — the walk's own peak was higher than the copy's transient. So memory mode can keep the plan in Electron at about 7 ms per million entries; spill mode is where the cage decides the design. | S2 measures the hand-over's peak inside Electron before building on it; S3's spill is read through native calls in Electron (a native-backed `ScanStore`), or spills only in web mode — decided with S2's measurements |
| R71 | **A file evicted after its scan is read as if it were local.** The duplicate pass trusts the scan's flags; a file iCloud or OneDrive makes online-only after the scan is opened, and downloaded, by a later duplicate pass over that scan | HIGH | Scans expire 30 minutes after they settle, which bounds the window; the pass reads only same-size candidates | **Retired 23 Sep 2026** where the native module loads (every build CI makes): just before it reads a bucket, the pass asks each file’s directory entry, never the file (`dataIsLocal`: `lstat`’s `st_flags & SF_DATALESS` on macOS, `FindFirstFileExW`’s attributes and reparse tag on Windows); a file whose data has left is counted in `notHashed`, one that cannot be asked about is not read. A copy without the module keeps the scan’s flags alone |
| R68 | **The last check before `Start-Process` reads the helper's and its folder's owner, not their access list**, so a folder owned by Administrators that still lets Users write would pass it | LOW | `elevationRefusal` runs first and tries rather than infers: it creates and removes a probe in the folder as the user, and a folder the user can write is refused whoever owns it; the owner check is a second line, against a change made between the two. Its file check opens for writing with every write right at once (libuv’s FILE_GENERIC_WRITE), so an access list that allows writing the data while refusing the attributes would pass it; `tests/mftHelperPath.test.ts` walks both checks on Windows with deny entries | Read the access list there too (`Get-Acl`: refuse any write right held by a SID that is not an administrator's) |
| R69 | **Nothing holds the helper open between the checks and `Start-Process`**, so a file swapped in between would be the one started | LOW | Swapping it needs write access to a folder only administrators can write (the checks above), and whoever has that has simpler routes | Open the helper with sharing that forbids writes (`FileShare.Read`) before the checks, and keep it open until the process has started |
| R70 | **The unelevated PowerShell that asks for elevation inherits the user's environment**: a profiler named there (`COR_ENABLE_PROFILING`, `COR_PROFILER`) loads into it and `PSModulePath` is searched; and `Start-Process -FilePath` reads `[` and `]` as wildcards | LOW | That PowerShell runs as the user, so anything it loads could already act as the user; Windows' prompt names the program it will run; the helper's path is TreeMap's install folder, which holds no brackets | Start PowerShell with a cleaned environment (no `COR_*`, a fixed `PSModulePath`) and elevate with `[Diagnostics.Process]::Start` (`Verb = 'runas'`), which takes the path literally — needs a live Windows run |

## I. Scope and sequencing

| # | Risk | Severity | Where it stands | Retired by |
| --- | --- | --- | --- | --- |
| R47 | **Building the walker before the governor** | MEDIUM | Phase order is fixed: governor first | — |
| R48 | **A phase claimed done on green tests without a reviewed diff** — the pattern every TreeMap session has recorded | HIGH | Each phase ends with an adversarial review fleet over the diff (`/ultra-review` plus purpose-built adversaries) before its check-in | every phase |
| R49 | **Windows MFT and the deep tier need the owner's decisions** (D7, D8) | LOW | Answered: both approved 18 Sep 2026. The MFT mode is built (W6: `8bbd8c8`, `b4dc6dc`); the deep tier is Phase 7's | W6 (MFT); Phase 7 (deep tier) |
| R50 | **The labelled image corpus may be unfair to hash-based signatures.** Its originals are gradient-dominated synthetic scenes; the legacy dHash collides across different originals within 10 bits, which is why it scores precision 0.18. A composite signature could pass this corpus and still fail on real photos, or the reverse | HIGH for Phase 6's claims | Phase 6 measures on this corpus **and** on a real library, opt-in and local, recording aggregates only; the corpus gains textured backgrounds in Phase 6 with the change recorded in its parameters, never silently | Phase 6 |
| R51 | **A first process launch after a pause is 15–27% slower** (measured on the gdu binary alone: 812 ms after a 3 s pause, ~600 ms with pauses, ~475 ms back to back), so a range-based reproducibility rule never records an engine that launches processes. The cause is not pinned (CPU frequency ramp after idle and `syspolicyd`'s per-launch assessment are the candidates) | MEDIUM | Every attempt is kept under `bench/results/`; the bare-binary figure is recorded in `CURRENT-STATE.md` §11.1; Phase 3's native path launches no process | Phase 3 |
